name: test-setup-cli-branch

on:
  workflow_dispatch:

jobs:
  alpine:
    runs-on: ubuntu-latest
    container:
      image: node:20-alpine
    strategy:
      fail-fast: false
      matrix:
        version:
          - 2.100.1
          - latest
    defaults:
      run:
        shell: sh
    steps:
      - uses: supabase/setup-cli@codex/use-apk-on-musl
        with:
          version: ${{ matrix.version }}

      - run: supabase --version
      - run: supabase --help
